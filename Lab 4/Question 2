#include<stdio.h>

int main() {
int num;
printf("Enter a num: ");
scanf("%d", &num);
if (num>=0 && num%2==0 && num%10==0) {
printf("Number is valid.\n"); 
}
else {
printf("Number is not valid.\n");
}
return 0;
}
